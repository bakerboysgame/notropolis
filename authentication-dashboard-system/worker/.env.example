# Backend Worker Environment Variables
# These are typically set in wrangler.toml [vars] section
# or as Cloudflare secrets for sensitive values

# ============================================
# REQUIRED: Set these for your project
# ============================================

# Brevo (Email) Configuration
# Sign up at https://www.brevo.com/ and get your API key
BREVO_API_KEY=your-brevo-api-key

# JWT Secret (generate a random 32+ character string)
# Command: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-at-least-32-characters

# Application URLs
CLIENT_URL=https://your-app.pages.dev
SERVER_URL=https://your-api.your-domain.workers.dev

# Brand Configuration (used in emails)
BRAND_NAME="Your App Name"

# ============================================
# OPTIONAL: Customize these if needed
# ============================================

# Rate Limiting
RATE_LIMIT_LOGIN=20
RATE_LIMIT_LOGIN_WINDOW=900000
RATE_LIMIT_API=100
RATE_LIMIT_API_WINDOW=60000

# Session Configuration
SESSION_IDLE_TIMEOUT=86400000

# CORS (defaults to CLIENT_URL if not set)
CORS_ORIGIN=https://your-app.pages.dev

# ============================================
# CLOUDFLARE BINDINGS (set in wrangler.toml)
# ============================================
# DB = D1 database binding
# RATE_LIMIT_KV = KV namespace for rate limiting

# ============================================
# EMAIL SENDER CONFIGURATION
# Set via wrangler.toml [vars] or Cloudflare secrets:
SENDER_NAME="Your App Name"
SENDER_EMAIL=noreply@yourdomain.com
# ============================================
