name = "notropolis-dashboard"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

[env.production]
name = "notropolis-dashboard"
vars = { ENVIRONMENT = "production" }

[env.preview]
name = "notropolis-dashboard-preview"
vars = { ENVIRONMENT = "preview" }
