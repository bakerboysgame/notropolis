# ================================================================================
# Multi-Tenant SaaS Authentication Dashboard Template
# Environment Variables Configuration
# ================================================================================
#
# Copy this file to .env and fill in your values
# For production, use Cloudflare secrets for sensitive values
#
# ================================================================================

# ==================== FRONTEND CONFIGURATION ====================
# These variables are used by the Vite frontend build

# API Configuration - URL of your Cloudflare Worker API
VITE_API_BASE_URL=http://localhost:8787

# Application Metadata
VITE_APP_NAME="Your App"
VITE_APP_VERSION="1.0.0"
VITE_APP_DESCRIPTION="Multi-tenant SaaS Dashboard"

# Environment (development, production, preview)
VITE_ENVIRONMENT=development

# ==================== BACKEND CONFIGURATION ====================
# These variables are set in worker/wrangler.toml or as Cloudflare secrets
# Listed here for reference

# -------------------- Cloudflare Account --------------------
# CLOUDFLARE_ACCOUNT_ID=your_account_id
# CLOUDFLARE_API_TOKEN=your_api_token (for deployment automation)

# -------------------- D1 Database --------------------
# D1_DATABASE_NAME=your-app-database
# D1_DATABASE_ID=your_d1_database_id

# -------------------- KV Namespace (Rate Limiting) --------------------
# KV_NAMESPACE_ID=your_kv_namespace_id
# KV_PREVIEW_NAMESPACE_ID=your_kv_preview_namespace_id

# -------------------- Application URLs --------------------
# CLIENT_URL=https://dashboard.your-domain.com
# SERVER_URL=https://api.your-domain.com
# CORS_ORIGIN=https://dashboard.your-domain.com

# -------------------- JWT Configuration --------------------
# JWT_SECRET=your_super_secure_jwt_secret_minimum_32_characters
# JWT_EXPIRES_IN=24h

# -------------------- Email Configuration --------------------
# Choose ONE email provider: Brevo OR Postmark

# Option A: Brevo (free tier: 300 emails/day)
# BREVO_API_KEY=your_brevo_api_key

# Option B: Postmark (better deliverability, built-in tracking)
# POSTMARK_SERVER_TOKEN=your_postmark_server_token

# Sender Configuration (used by both providers)
# SENDER_NAME=Your App
# SENDER_EMAIL=app@your-domain.com

# -------------------- Rate Limiting --------------------
# RATE_LIMIT_LOGIN=20           # Max login attempts per 15 min window
# RATE_LIMIT_LOGIN_WINDOW=900   # Window in seconds
# RATE_LIMIT_API=100            # Max API requests per 1 min window
# RATE_LIMIT_API_WINDOW=60      # Window in seconds
# SESSION_IDLE_TIMEOUT=1800000  # Session idle timeout in ms (30 min)

# -------------------- Session Configuration --------------------
# SESSION_TIMEOUT=3600000       # Session timeout in ms (1 hour)

# -------------------- Branding --------------------
# BRAND_NAME=Your App

# ==================== OPTIONAL R2 STORAGE ====================
# Uncomment if using R2 for file storage
# R2_BUCKET_NAME=your-app-data

# ==================== SECRETS (Set via Cloudflare Dashboard) ====================
# These should NEVER be committed to version control
# Set them using: wrangler secret put SECRET_NAME
#
# Required secrets:
#   - JWT_SECRET: Strong random string for JWT signing
#   - CORS_ORIGIN: Allowed frontend origin for CORS
#
# Email provider (choose ONE):
#   - BREVO_API_KEY: Your Brevo API key for email sending
#   - POSTMARK_SERVER_TOKEN: Your Postmark Server API Token
#
# Optional secrets (if using R2):
#   - R2_ACCESS_KEY_ID: R2 access key
#   - R2_SECRET_ACCESS_KEY: R2 secret key
